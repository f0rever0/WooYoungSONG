import { DefaultTheme } from 'styled-components';

const fontSizes = {
  header: '5rem',
  title: '3rem',
  subTitle: '2rem',
  text: '1rem'
};

const colors = {
  mainColor: '#647295',
  subColor: '#F2EBE5',
  pointColor: '#9F496E',
  buttonColor: '#2B262D',
  grayColor: '#777',
};

const theme: DefaultTheme = {
  fontSizes,
  colors,
};
export default theme;